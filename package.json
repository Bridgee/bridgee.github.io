{
  "name": "bridge-homepage",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy-clean": "astro build && gh-pages -d dist -b gh-pages --dotfiles --src \"**/*\" --remove \"thoughts\"",
    "deploy": "astro build && gh-pages -d dist -b gh-pages --dotfiles --no-history",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.12.9",
    "gh-pages": "^6.3.0"
  }
}
