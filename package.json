{
  "name": "bridge-homepage",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "validate:external": "node scripts/validate-external-links.mjs",
    "check": "astro check",
    "build": "astro build",
    "validate": "npm run validate:content && npm run check && npm run build && npm run validate:build",
    "preview": "astro preview",
    "deploy": "node deploy-script.js",
    "astro": "astro"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.7",
    "gh-pages": "^6.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
